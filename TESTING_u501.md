# Тестирование u501_import_from_ut

## Подготовка к тестированию

### 1. Запуск Backend

```bash
# В корневой директории проекта
cargo run --bin backend
```

Backend запустится на `http://localhost:3000`

### 2. Запуск Frontend

```bash
# В отдельном терминале
trunk serve
```

Frontend будет доступен на `http://localhost:8080`

Trunk автоматически проксирует запросы `/api/*` на `http://localhost:3000/api/*`

### 3. Создание тестовых данных

**Вариант A: Через curl**
```bash
curl -X POST http://localhost:3000/api/connection_1c/testdata
```

**Вариант B: Через браузер**
1. Откройте `http://localhost:8080`
2. В левом меню нажмите "1C Connections"
3. Проверьте что появились тестовые подключения

## Тестирование через UI

### Шаг 1: Открыть виджет импорта

1. Откройте `http://localhost:8080` в браузере
2. В левом меню найдите секцию **"Операции"**
3. Нажмите на **"Импорт из УТ 11"**

Должен открыться виджет с:
- Dropdown выбора подключения
- Checkbox для выбора агрегатов (пока только Organizations)
- Кнопка "Запустить импорт"

### Шаг 2: Настроить подключение к реальной 1С

Если у вас есть доступ к реальному серверу 1С УТ 11:

1. Перейдите в "1C Connections"
2. Создайте новое подключение:
   - **Описание**: "Тестовая база УТ 11"
   - **URL**: `http://your-server:port/base/odata/standard.odata`
   - **Login**: ваш логин
   - **Password**: ваш пароль
   - **Is Primary**: true (если основное)

3. Сохраните

### Шаг 3: Запустить импорт

1. Вернитесь в **"Импорт из УТ 11"**
2. Выберите подключение из dropdown
3. Убедитесь что выбран checkbox "a002_organization - Организации"
4. Нажмите **"Запустить импорт"**

### Шаг 4: Мониторинг прогресса

После запуска вы увидите:

1. **Session ID** - уникальный идентификатор сессии
2. **Статус** - цвет меняется в зависимости от состояния:
   - 🔵 Синий - Running (выполняется)
   - 🟢 Зеленый - Completed (успешно)
   - 🟡 Желтый - CompletedWithErrors (с ошибками)
   - 🔴 Красный - Failed (провален)

3. **Общая статистика**:
   - Обработано
   - Создано
   - Обновлено
   - Ошибок

4. **Прогресс по агрегатам**:
   - Progress bar (если известно общее количество)
   - Детальная статистика по каждому агрегату
   - Текущий процент выполнения

5. **Ошибки** (если есть):
   - Список ошибок с деталями
   - Время возникновения

### Шаг 5: Проверка результатов

После завершения импорта:

1. Перейдите в **"Организации"** в левом меню
2. Убедитесь что данные загрузились
3. Проверьте детали организаций (ИНН, КПП, полное наименование)

## Тестирование через API (без UI)

### Тест 1: Запуск импорта

```bash
# Получить ID подключения
curl http://localhost:3000/api/connection_1c

# Скопировать ID из ответа и использовать ниже
curl -X POST http://localhost:3000/api/u501/import/start \
  -H "Content-Type: application/json" \
  -d '{
    "connection_id": "ваш-uuid-здесь",
    "target_aggregates": ["a002_organization"],
    "mode": "interactive"
  }'
```

Ответ:
```json
{
  "session_id": "session-uuid",
  "status": "started",
  "message": "Импорт запущен"
}
```

### Тест 2: Получение прогресса

```bash
# Заменить SESSION_ID на полученный из предыдущего шага
curl http://localhost:3000/api/u501/import/SESSION_ID/progress
```

Ответ (пример):
```json
{
  "session_id": "uuid",
  "status": "running",
  "started_at": "2025-01-15T10:00:00Z",
  "completed_at": null,
  "aggregates": [
    {
      "aggregate_index": "a002_organization",
      "aggregate_name": "Организации",
      "status": "running",
      "processed": 50,
      "total": 100,
      "inserted": 10,
      "updated": 40,
      "errors": 0,
      "current_item": null
    }
  ],
  "total_processed": 50,
  "total_inserted": 10,
  "total_updated": 40,
  "total_errors": 0,
  "errors": []
}
```

### Тест 3: Polling до завершения

```bash
# Bash script для автоматического polling
SESSION_ID="your-session-id"

while true; do
  response=$(curl -s http://localhost:3000/api/u501/import/$SESSION_ID/progress)
  status=$(echo $response | grep -o '"status":"[^"]*"' | cut -d'"' -f4)

  echo "Status: $status"
  echo "$response" | jq '.'

  if [[ "$status" == "completed" || "$status" == "failed" || "$status" == "completed_with_errors" ]]; then
    break
  fi

  sleep 2
done
```

### Тест 4: Проверка данных

```bash
# Получить список организаций
curl http://localhost:3000/api/organization | jq '.'

# Получить конкретную организацию
curl http://localhost:3000/api/organization/UUID | jq '.'
```

## Сценарии тестирования

### Сценарий 1: Успешный импорт

**Условия:**
- Сервер 1С доступен
- Учетные данные верны
- Справочник Organizations содержит корректные данные

**Ожидаемый результат:**
- Статус: `Completed`
- Все записи обработаны
- Нет ошибок
- Данные появились в таблице Organizations

### Сценарий 2: Импорт с ошибками валидации

**Условия:**
- В 1С есть организации с некорректными ИНН/КПП

**Ожидаемый результат:**
- Статус: `CompletedWithErrors`
- Часть записей обработана
- В `errors` массиве список проблемных записей
- Корректные записи загружены

### Сценарий 3: Ошибка подключения

**Условия:**
- Неверный URL или учетные данные
- Сервер 1С недоступен

**Ожидаемый результат:**
- Статус: `Failed`
- В errors сообщение об ошибке подключения
- Данные не изменились

### Сценарий 4: Повторный импорт (upsert)

**Условия:**
- Данные уже загружены ранее
- Запускается повторный импорт

**Ожидаемый результат:**
- Статус: `Completed`
- `total_inserted`: 0 (если нет новых)
- `total_updated`: N (количество обновленных)
- Существующие записи обновлены

## Проверка логов

### Backend логи

```bash
# Запустить с debug логами
RUST_LOG=debug cargo run --bin backend
```

Ищите в логах:
```
INFO Starting import for session: uuid
INFO Fetching OData from: http://...
INFO Organizations import completed: processed=200, inserted=50, updated=150
```

### Browser console (F12)

Проверьте:
- Network tab - запросы к API
- Console - ошибки JavaScript
- Application - локальное хранилище (если используется)

## Проблемы и решения

### Проблема: "No window object"
**Решение**: Убедитесь что код выполняется в браузере, а не на сервере

### Проблема: CORS ошибки
**Решение**: Backend настроен на `allow_origin(Any)`, должно работать из любого источника

### Проблема: Импорт зависает на "Running"
**Решение**:
1. Проверьте backend логи
2. Увеличьте timeout в `ut_odata_client.rs`
3. Уменьшите `page_size` в executor

### Проблема: Пустой dropdown подключений
**Решение**:
1. Создайте тестовые данные: `POST /api/connection_1c/testdata`
2. Проверьте что backend запущен
3. Проверьте Network tab в браузере

## Метрики производительности

Отслеживайте:
- **Скорость**: записей/минуту
- **Использование памяти**: в task manager
- **Latency**: время ответа OData
- **Успешность**: % успешных записей

Типичные значения:
- 100-500 записей/мин (зависит от сервера 1С)
- ~1KB памяти на сессию
- Latency 100-500ms на запрос

## Чек-лист перед production

- [ ] Протестировать с реальным сервером 1С
- [ ] Проверить работу с большим объемом данных (1000+ записей)
- [ ] Протестировать сценарий с ошибками
- [ ] Проверить повторный импорт (upsert)
- [ ] Убедиться что старые сессии очищаются
- [ ] Добавить обработку отмены импорта
- [ ] Добавить WebSocket для real-time обновлений
- [ ] Добавить сохранение истории в БД
- [ ] Настроить production CORS
- [ ] Добавить rate limiting

## Следующие шаги

1. Добавить больше агрегатов (Products, Counterparties)
2. Реализовать фоновый режим с планировщиком
3. Добавить API для отмены импорта
4. Создать dashboard с историей импортов
5. Добавить уведомления об ошибках
