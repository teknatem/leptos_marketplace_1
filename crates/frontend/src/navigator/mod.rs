//! Navigator subsystem — landing pages with curated cross-domain links.
//!
//! Each page presents a matrix of links grouped into blocks (rows) and
//! categories (columns, e.g. marketplaces). Three render modes per page:
//! Brief, Detailed, Table — each in its own file.

pub mod marketplace;
pub mod shared;
