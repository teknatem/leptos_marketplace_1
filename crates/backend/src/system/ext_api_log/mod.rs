//! Лог входящих вызовов внешнего API (`/api/ext/v1/*`).
//!
//! Пишет строку на каждый вызов: кто позвал, что запросил, что получил, сколько это
//! заняло. Нужен для контроля корректности интеграций (какие параметры шлют, какие
//! ошибки получают) и их нагрузки на систему.
//!
//! Внутренний API SPA сюда **не попадает**: слой висит на ext-саброутере, а не глобально.

pub mod middleware;
pub mod repository;
pub mod service;
