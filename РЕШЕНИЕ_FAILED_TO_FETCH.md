# ✅ Решение: Failed to fetch при авторизации

## Проблема

Backend запускается успешно:

```
✓ Server successfully bound to 0.0.0.0:3000
```

Но при попытке авторизации во frontend:

```
Ошибка входа: Failed to send request: TypeError: Failed to fetch
```

---

## 🎯 Причина

**Frontend пытается подключиться не к тому адресу!**

Frontend автоматически определяет адрес backend по адресу в браузере:

- Если открыли `file:///C:/...` → backend на `:3000` (пусто) ❌
- Если открыли `http://localhost/...` → backend на `localhost:3000` ❌
- Нужно открывать `http://<IP-СЕРВЕРА>:3000` → backend на `<IP-СЕРВЕРА>:3000` ✅

---

## 🚀 Быстрое решение (5 минут)

### Шаг 1: Узнайте IP адрес сервера

На Windows Server выполните в командной строке:

```cmd
ipconfig | findstr IPv4
```

**Пример вывода:**

```
   IPv4-адрес. . . . . . . . . . . . : 192.168.1.100
```

Запомните этот IP: **192.168.1.100**

---

### Шаг 2: Откройте порт в файрволе

PowerShell **от имени администратора**:

```powershell
New-NetFirewallRule -DisplayName "Marketplace Backend" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

Должно показать:

```
Name                  : {guid}
DisplayName           : Marketplace Backend
Enabled               : True
```

---

### Шаг 3: Проверьте доступность backend

**На самом сервере** откройте браузер:

```
http://localhost:3000
```

Должна открыться страница приложения.

**С другого компьютера** откройте браузер:

```
http://192.168.1.100:3000
```

(замените на ваш IP из Шага 1)

Должна открыться та же страница.

---

### Шаг 4: Убедитесь, что папка dist/ на месте

Проверьте структуру:

```
C:\Users\udv\Desktop\MPI\
├── backend.exe          ✅ Есть
├── config.toml          ✅ Есть
├── dist\                ⚠️ Проверьте!
│   ├── index.html
│   ├── pkg\
│   └── ...
└── data\
    └── app.db
```

**Если папки `dist/` нет:**

1. Соберите frontend на компьютере разработки:

   ```cmd
   cd crates/frontend
   trunk build --release
   ```

2. Скопируйте всю папку `dist/` на сервер рядом с `backend.exe`

---

## ✅ Проверка результата

1. Откройте браузер **с другого компьютера**
2. Введите адрес: `http://192.168.1.100:3000` (ваш IP)
3. Должна открыться страница входа
4. Введите:
   - **Логин:** admin
   - **Пароль:** admin
5. Должен произойти вход в систему!

---

## 🔍 Если все еще не работает

### Проверка 1: Backend запущен?

На сервере проверьте:

```cmd
tasklist | findstr backend.exe
```

Должна быть строка:

```
backend.exe         12345 Console                 1     45,678 K
```

### Проверка 2: Порт открыт?

На сервере:

```cmd
netstat -ano | findstr :3000
```

Должна быть строка:

```
TCP    0.0.0.0:3000           0.0.0.0:0              LISTENING       12345
```

### Проверка 3: Файрвол разрешает?

PowerShell:

```powershell
Get-NetFirewallRule -DisplayName "*Marketplace*" | Select-Object DisplayName, Enabled, Direction
```

Должно быть:

```
DisplayName            Enabled Direction
-----------            ------- ---------
Marketplace Backend    True    Inbound
```

### Проверка 4: Браузер блокирует?

Откройте **F12** → **Console**

Ищите ошибки:

- `net::ERR_CONNECTION_REFUSED` → Backend не запущен или файрвол
- `net::ERR_NAME_NOT_RESOLVED` → Неверный IP адрес
- `Mixed Content` → Используйте http:// для обоих (frontend и backend)

---

## 📋 Диагностика для специалиста

Если проблема не решена, соберите информацию:

**На сервере выполните:**

```cmd
echo "=== Network Info ===" > diagnostic.txt
ipconfig /all >> diagnostic.txt
echo. >> diagnostic.txt

echo "=== Port Status ===" >> diagnostic.txt
netstat -ano | findstr :3000 >> diagnostic.txt
echo. >> diagnostic.txt

echo "=== Firewall Rules ===" >> diagnostic.txt
powershell "Get-NetFirewallRule -DisplayName '*3000*' | Format-List" >> diagnostic.txt
echo. >> diagnostic.txt

echo "=== Backend Process ===" >> diagnostic.txt
tasklist | findstr backend >> diagnostic.txt

notepad diagnostic.txt
```

**В браузере (на клиенте):**

1. Откройте F12 → Network
2. Попробуйте авторизоваться
3. Скриншот ошибки
4. Правой кнопкой на failed запрос → Copy → Copy as cURL

---

## 💡 Важные моменты

### ✅ ПРАВИЛЬНО:

```
Backend на: 192.168.1.100:3000
Frontend открыт через: http://192.168.1.100:3000
→ Подключается к: http://192.168.1.100:3000/api/...
✅ РАБОТАЕТ!
```

### ❌ НЕПРАВИЛЬНО:

```
Backend на: 192.168.1.100:3000
Frontend открыт через: file:///C:/Users/udv/Desktop/dist/index.html
→ Подключается к: :3000/api/... (пустой hostname)
❌ НЕ РАБОТАЕТ!
```

```
Backend на: 192.168.1.100:3000
Frontend открыт через: http://localhost/dist/
→ Подключается к: http://localhost:3000/api/...
❌ НЕ РАБОТАЕТ! (Backend не на localhost)
```

---

## 🎉 Готово!

После выполнения всех шагов:

- ✅ Backend доступен по сети
- ✅ Frontend подключается к правильному адресу
- ✅ Авторизация работает
- ✅ Приложение полностью функционально

**Основной адрес приложения:** `http://192.168.1.100:3000`

(замените на ваш IP адрес)

---

## 📚 Дополнительная информация

Для более подробного объяснения проблемы и альтернативных решений см.:

- **FRONTEND_BACKEND_CONNECTION.md** - полное руководство по подключению
- **DEPLOYMENT_WINDOWS_SERVER.md** - руководство по развертыванию
