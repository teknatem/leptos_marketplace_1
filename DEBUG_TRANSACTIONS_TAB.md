# Отладка вкладки "Транзакции" в a010 Details

## Что было сделано:

1. **Backend**: Добавлен эндпоинт `/api/ozon_transactions/by-posting/:posting_number`

   - Декодирует URL-кодированный posting_number
   - Логирует количество найденных транзакций

2. **Frontend**: Добавлена вкладка "💳 Транзакции"
   - URL-кодирует posting_number перед отправкой
   - Подробно логирует процесс загрузки

## Как проверить проблему:

### 1. Откройте консоль браузера (F12)

### 2. Откройте детали постинга с posting_number = `0147833718-0178`

### 3. Проверьте логи в консоли:

- `Loading transactions for posting_number: ...` - какой posting_number отправляется
- `Request URL: ...` - какой URL формируется
- `Transactions response status: ...` - статус ответа
- `Transactions response: ...` - что вернул сервер
- `Successfully parsed N transactions` - сколько транзакций получено

### 4. Проверьте логи бэкенда:

```
Getting transactions for posting_number: <decoded> (original: <encoded>)
A014 get_by_posting_number: found N items for posting <posting_number>
Found N transactions for posting_number: <posting_number>
```

## Возможные проблемы:

### Проблема 1: Разные форматы posting_number

- **a010** хранит `document_no` (например: `0147833718-0178`)
- **a014** хранит `posting_number` (должен быть такой же)

**Решение**: Проверить в консоли браузера, какой document_no используется

### Проблема 2: Данных нет в базе

**Решение**:

1. Запустить импорт транзакций через u502
2. Убедиться, что транзакции для этого posting_number действительно есть

### Проблема 3: URL-кодирование

**Решение**: Уже исправлено - posting_number теперь URL-кодируется

## Тестовый запрос напрямую:

Попробуйте в браузере:

```
http://localhost:3000/api/ozon_transactions/by-posting/0147833718-0178
```

Должен вернуть JSON массив с транзакциями.

## SQL-запрос для проверки данных:

```sql
-- Найти posting_number, похожий на искомый
SELECT posting_number, COUNT(*) as cnt
FROM a014_ozon_transactions
WHERE posting_number LIKE '%0147833718-0178%'
  AND is_deleted = 0;

-- Показать несколько posting_number для сравнения
SELECT DISTINCT posting_number
FROM a014_ozon_transactions
WHERE is_deleted = 0
LIMIT 20;
```
