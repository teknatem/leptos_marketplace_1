# Thaw UI Standards

**Дата:** 2025-12-23
**Версия:** 1.0
**Статус:** ✅ Принято

## 🎯 Обзор
Проект использует библиотеку **Thaw UI (версия 0.5.0-beta)** для реализации современных и реактивных компонентов пользовательского интерфейса в Leptos 0.8.

## 🏗️ Ключевые компоненты

В проекте предпочтительно использовать компоненты Thaw вместо стандартных HTML элементов:

| Компонент | Назначение | Пример использования |
|-----------|------------|----------------------|
| `Button` | Кнопки с поддержкой стилей | `<Button appearance=ButtonAppearance::Primary on_click=...>...</Button>` |
| `Input` | Поля ввода текста | `<Input value=rw_signal placeholder="..." />` |
| `Textarea` | Многострочный ввод | `<Textarea value=rw_signal placeholder="..." attr:rows=10 />` |
| `Select` | Выпадающие списки | `<Select value=rw_signal><option ...>...</option></Select>` |
| `Checkbox` | Чекбоксы (модель bool) | `<Checkbox checked=rw_signal label="..." />` |
| `Badge` | Индикаторы статуса | `<Badge appearance=BadgeAppearance::Tint color=BadgeColor::Success>...</Badge>` |
| `Spinner` | Индикатор загрузки | `<Spinner />` |
| `Flex` | Гибкая разметка (Layout) | `<Flex justify=FlexJustify::SpaceBetween align=FlexAlign::Center>...</Flex>` |
| `Space` | Отступы между элементами | `<Space gap=SpaceGap::Medium>...</Space>` |

## 💡 Особенности интеграции с Leptos 0.8

1.  **Reactivity (Model)**: Большинство компонентов Thaw (Input, Select, Checkbox) принимают `RwSignal` напрямую в проп `value` или `checked`. Это автоматически обеспечивает двустороннюю привязку данных.
2.  **Custom Attributes**: Для установки стандартных HTML атрибутов, которые не выведены в пропсы компонента (например, `rows` для `Textarea` или `style` для `Button`), используйте префикс `attr:`:
    ```rust
    <Textarea attr:rows=5 ... />
    <Button attr:style="color: red;" ... />
    ```
3.  **Event Handlers**: Для кнопок используйте проп `on_click`, передавая `Callback`. Для перехвата события с типом используйте аннотации:
    ```rust
    on_click=move |e: ev::MouseEvent| {
        e.stop_propagation();
        // логика
    }
    ```
4.  **Layout**: Используйте `Flex` и `Space` вместо ручного написания CSS для выравнивания элементов. Это гарантирует консистентность отступов и поведения на разных экранах.

## 🎨 Стилизация
Стили Thaw UI интегрированы с темой проекта (Dark/Light). При необходимости переопределения стилей рекомендуется использовать CSS переменные из `base.css` или специализированные классы BEM, если компонент встроен в таблицу.

