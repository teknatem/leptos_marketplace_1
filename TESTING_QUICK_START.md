# Быстрый старт: Тестирование u501_import_from_ut

## ✅ Исправления в последней версии

### Правильное формирование OData URL

**Было:**
```
http://79.137.226.70:8787/TradeSklad/Catalog_Организации  ❌
```

**Стало:**
```
http://79.137.226.70:8787/TradeSklad/odata/standard.odata/Catalog_Организации  ✅
```

**Логика:**
- Если в Connection1C.url уже есть `/odata/`, используется как есть
- Если нет, добавляется `/odata/standard.odata/` автоматически

### Отображение пути в UI

В виджете теперь показывается:
- 📍 **Путь загрузки** - полный URL с которого будут загружаться данные
- 📦 **OData коллекция** - название коллекции (Catalog_Организации)

---

## Шаг 1: Запустить Backend

```bash
cargo run --bin backend
```

Должно появиться:
```
INFO Attempting to bind server to http://0.0.0.0:3000
INFO Server successfully bound to 0.0.0.0:3000
```

---

## Шаг 2: Запустить Frontend

В отдельном терминале:
```bash
trunk serve
```

Должно появиться:
```
INFO 📦 Serving at: http://127.0.0.1:8080
```

Откройте в браузере: **http://localhost:8080**

---

## Шаг 3: Создать подключение к вашей базе 1С

### Вариант A: Через UI

1. В левом меню нажмите **"1C Connections"**
2. Нажмите кнопку создания (или добавьте вручную)
3. Заполните форму:

```
Код: УТ-ПРОД
Описание: Основная база УТ 11
URL: http://79.137.226.70:8787/TradeSklad
Логин: ваш_логин
Пароль: ваш_пароль
Is Primary: ✓
```

**ВАЖНО:** В URL указывайте только путь до базы, **БЕЗ** `/odata/standard.odata/`
- ✅ Правильно: `http://79.137.226.70:8787/TradeSklad`
- ❌ Неправильно: `http://79.137.226.70:8787/TradeSklad/odata/standard.odata`

### Вариант B: Через curl

```bash
curl -X POST http://localhost:3000/api/connection_1c \
  -H "Content-Type: application/json" \
  -d '{
    "code": "УТ-ПРОД",
    "description": "Основная база УТ 11",
    "url": "http://79.137.226.70:8787/TradeSklad",
    "login": "ваш_логин",
    "password": "ваш_пароль",
    "isPrimary": true
  }'
```

---

## Шаг 4: Запустить импорт через UI

1. В левом меню найдите секцию **"Операции"**
2. Нажмите **"Импорт из УТ 11"**
3. Выберите подключение из dropdown
4. Проверьте **"Путь загрузки"** - должен быть:
   ```
   http://79.137.226.70:8787/TradeSklad/odata/standard.odata/Catalog_Организации
   ```
5. Убедитесь что checkbox **"a002_organization - Организации"** выбран
6. Нажмите **"Запустить импорт"**

### Что вы увидите:

**Сразу после запуска:**
```
Session ID: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
Статус: Running (синий)
Обработано: 0 | Создано: 0 | Обновлено: 0 | Ошибок: 0
```

**В процессе (обновляется каждые 2 секунды):**
```
┌─────────────────────────────────────────┐
│ a002_organization - Организации         │
│ 50 / 100 (50%)                          │
│ ██████████░░░░░░░░░░                    │
│ Создано: 10 | Обновлено: 40 | Ошибок: 0│
└─────────────────────────────────────────┘
```

**После завершения:**
```
Статус: Completed (зеленый)
Обработано: 100 | Создано: 20 | Обновлено: 80 | Ошибок: 0
```

---

## Шаг 5: Проверить результаты

1. В левом меню нажмите **"Организации"**
2. Должна загрузиться таблица с данными из 1С
3. Проверьте что:
   - Есть ИНН
   - Есть КПП
   - Полное наименование заполнено

---

## Тестирование через API (альтернатива UI)

### 1. Получить ID подключения

```bash
curl http://localhost:3000/api/connection_1c | jq '.[] | {id: .id, description: .description}'
```

Скопируйте `id` нужного подключения.

### 2. Запустить импорт

```bash
curl -X POST http://localhost:3000/api/u501/import/start \
  -H "Content-Type: application/json" \
  -d '{
    "connection_id": "ВСТАВЬТЕ_UUID_СЮДА",
    "target_aggregates": ["a002_organization"],
    "mode": "interactive"
  }' | jq '.'
```

Ответ:
```json
{
  "session_id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "status": "started",
  "message": "Импорт запущен"
}
```

### 3. Мониторить прогресс

```bash
# Заменить SESSION_ID на полученный выше
curl http://localhost:3000/api/u501/import/SESSION_ID/progress | jq '.'
```

### 4. Автоматический polling (Bash script)

```bash
SESSION_ID="ВСТАВЬТЕ_SESSION_ID_СЮДА"

while true; do
  response=$(curl -s http://localhost:3000/api/u501/import/$SESSION_ID/progress)
  status=$(echo $response | jq -r '.status')
  processed=$(echo $response | jq -r '.total_processed')

  echo "[$status] Обработано: $processed"

  if [[ "$status" == "completed" || "$status" == "failed" ]]; then
    echo "$response" | jq '.'
    break
  fi

  sleep 2
done
```

---

## Проверка логов Backend

В терминале backend должны появиться записи:

```
INFO Starting import for session: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
INFO Fetching OData from: http://79.137.226.70:8787/TradeSklad/odata/standard.odata/Catalog_Организации?$top=100&$skip=0
INFO Organizations import completed: processed=100, inserted=20, updated=80
INFO Import completed for session: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

Для более детальных логов:
```bash
RUST_LOG=debug cargo run --bin backend
```

---

## Проверка Browser Console (F12)

### Network Tab
Проверьте запросы:
```
✅ GET  http://localhost:3000/api/connection_1c
✅ POST http://localhost:3000/api/u501/import/start
✅ GET  http://localhost:3000/api/u501/import/<session>/progress
```

### Console Tab
Не должно быть ошибок. Возможные warnings про deprecated методы - это нормально.

---

## Частые проблемы и решения

### Проблема 1: "OData request failed with status 401"

**Причина:** Неверные учетные данные

**Решение:**
1. Проверьте логин/пароль в Connection1C
2. Убедитесь что пользователь имеет права на чтение справочников
3. Проверьте что пароль не изменился

### Проблема 2: "OData request failed with status 404"

**Причина:** Неверный URL или коллекция не существует

**Решение:**
1. Проверьте URL в Connection1C (должен быть без `/odata/`)
2. Проверьте что база УТ 11, а не другая конфигурация
3. Проверьте в браузере: `http://79.137.226.70:8787/TradeSklad/odata/standard.odata/`

### Проблема 3: "Connection timed out"

**Причина:** Сервер 1С недоступен

**Решение:**
1. Проверьте что сервер доступен: `ping 79.137.226.70`
2. Проверьте что порт открыт: `telnet 79.137.226.70 8787`
3. Увеличьте timeout в `ut_odata_client.rs` (сейчас 30 сек)

### Проблема 4: Пустой dropdown подключений

**Причина:** Нет созданных подключений

**Решение:**
```bash
# Создать тестовое подключение
curl -X POST http://localhost:3000/api/connection_1c/testdata

# Или создать реальное (см. Шаг 3)
```

### Проблема 5: "ИНН должен содержать 10 или 12 цифр"

**Причина:** В 1С есть организации с некорректным ИНН

**Решение:**
- Это нормально, импорт продолжится
- Проблемные записи будут в списке ошибок
- Корректные записи загрузятся

---

## Проверка правильности URL

### Тест 1: Метаданные OData

```bash
curl http://79.137.226.70:8787/TradeSklad/odata/standard.odata/ \
  -u login:password
```

Должен вернуться XML с метаданными.

### Тест 2: Коллекция Organizations

```bash
curl "http://79.137.226.70:8787/TradeSklad/odata/standard.odata/Catalog_Организации?\$top=1" \
  -u login:password \
  -H "Accept: application/json" | jq '.'
```

Должен вернуться JSON с одной организацией.

---

## Следующие шаги

После успешного импорта Organizations:

1. Добавить импорт Products (Номенклатура)
2. Добавить импорт Counterparties (Контрагенты)
3. Настроить фоновый режим с расписанием
4. Добавить WebSocket для real-time обновлений
5. Создать dashboard с историей импортов

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи backend
2. Проверьте Network tab в браузере
3. Убедитесь что 1С доступна
4. Проверьте формат URL (без `/odata/` в конце)

Документация:
- [README_u501.md](README_u501.md) - Полное руководство
- [TESTING_u501.md](TESTING_u501.md) - Детальные сценарии тестирования
- [naming-conventions.md](memory-bank/naming-conventions.md) - Архитектура
