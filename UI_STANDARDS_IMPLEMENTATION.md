# UI Standards Implementation - Итоги внедрения

**Дата:** 2025-12-19  
**Статус:** ✅ Успешно реализовано

---

## 🎯 Цели проекта

Разработать единый практичный стандарт для всех страниц в центральном блоке (вкладки):

- Два типа таблиц: простые (справочники) и сложные (документы)
- Единые чекбоксы во всех таблицах
- Гибкая система отображения итогов
- Строгое следование BEM методологии
- CSS-система для быстрого развития без переусложнения

---

## ✅ Что реализовано

### 1. Документация (3 файла)

#### [`memory-bank/architecture/table-standards.md`](memory-bank/architecture/table-standards.md)

- Сравнительная таблица двух типов
- Критерии выбора типа таблицы
- Полные примеры кода для каждого типа
- BEM чеклист для code review
- Эталонные примеры

#### [`memory-bank/architecture/detail-form-standard.md`](memory-bank/architecture/detail-form-standard.md)

- Структура форм детальных записей
- Стандартные закладки (Общие, Товары, Raw JSON, Проекции)
- Компонент field-row для полей
- CSS классы и примеры кода

#### [`memory-bank/architecture/UI_STANDARDS_README.md`](memory-bank/architecture/UI_STANDARDS_README.md)

- Краткое руководство по применению
- Примеры использования компонентов
- Инструкции по миграции остальных списков

### 2. Backend (серверные итоги)

#### Обновлённые handlers

**`crates/backend/src/handlers/a016_ym_returns.rs`**

```rust
// Новая структура
pub struct YmReturnsTotals {
    pub total_records: usize,
    pub sum_items: i32,
    pub sum_amount: f64,
    pub returns_count: usize,
    pub unredeemed_count: usize,
}

// Новая функция
async fn calculate_totals(query: &YmReturnsListQuery) -> Result<YmReturnsTotals>
```

**`crates/backend/src/handlers/a012_wb_sales.rs`**

```rust
// Новая структура
pub struct WbSalesTotals {
    pub total_records: usize,
    pub sum_quantity: i32,
    pub sum_for_pay: f64,
    pub sum_retail_amount: f64,
}

// Новая функция
async fn calculate_wb_sales_totals(query: &WbSalesListQuery) -> Result<WbSalesTotals>
```

#### Обновлённые response structures

```rust
pub struct PaginatedResponse {
    pub items: Vec<T>,
    pub total: usize,
    pub page: usize,
    pub page_size: usize,
    pub total_pages: usize,
    pub totals: Option<ServerTotals>,  // <- НОВОЕ ПОЛЕ
}
```

### 3. Frontend компоненты

#### [`crates/frontend/src/shared/components/table_checkbox.rs`](crates/frontend/src/shared/components/table_checkbox.rs)

- Единый стиль чекбоксов
- Фиксированная ширина 40px
- Stop propagation (клик не открывает detail)
- BEM классы

**Использование:**

```rust
<TableCheckbox
    checked=Signal::derive(move || selected.get().contains(&id))
    on_change=Callback::new(move |checked| toggle_select(id, checked))
/>
```

#### [`crates/frontend/src/shared/components/table_totals_row.rs`](crates/frontend/src/shared/components/table_totals_row.rs)

- Гибкое содержимое через children slot
- Легко включить/выключить
- BEM класс `.table__totals-row`

**Использование:**

```rust
{move || {
    if let Some(totals) = state.get().server_totals {
        view! {
            <TableTotalsRow>
                <td class="table__cell--checkbox"></td>
                <td>{format!("Всего: {}", totals.total_records)}</td>
                <td class="table__cell--right">{format_number(totals.sum_amount)}</td>
            </TableTotalsRow>
        }.into_any()
    } else {
        view! { <></> }.into_any()
    }
}}
```

### 4. CSS обновления

#### [`crates/frontend/static/themes/core/components.css`](crates/frontend/static/themes/core/components.css)

Добавлены/обновлены BEM классы:

```css
/* Чекбоксы - фиксированная ширина 40px */
.table__cell--checkbox {
  width: 40px;
  min-width: 40px;
  max-width: 40px;
}
.table__header-cell--checkbox {
  width: 40px;
  min-width: 40px;
  max-width: 40px;
}
.table__checkbox {
  width: 16px;
  height: 16px;
  accent-color: var(--color-primary);
}

/* Итоги - sticky, серверные данные */
.table__totals-row {
  position: sticky;
  top: var(--table-header-height);
}

/* Выравнивание чисел */
.table__cell--right {
  text-align: right;
}
.table__header-cell--right {
  text-align: right;
}
```

**Все используют CSS-переменные:**

- `padding: var(--spacing-xs)` вместо `4px`
- `font-size: var(--font-size-sm)` вместо `12px`
- `color: var(--color-primary)` вместо `#2563eb`

### 5. Обновлённые списки

#### Простые таблицы (без пагинации) ✅

- ✅ **a002_organization** - Организации
- ✅ **a005_marketplace** - Маркетплейсы
- ✅ **a006_connection_mp** - Подключения к маркетплейсам
- ✅ **a001_connection_1c** - Подключения 1C
- ✅ **a007_marketplace_product** - Продукты (частично)

**Изменения:**

- TableCheckbox компонент
- BEM классы для чекбоксов
- Модальное окно для редактирования

#### Сложные таблицы (пагинация + итоги) ✅

- ✅ **a016_ym_returns** - Возвраты Яндекс ⭐ **ЭТАЛОН**

  - TableCheckbox + TableTotalsRow
  - Серверные итоги (total_records, sum_items, sum_amount, returns/unredeemed counts)
  - BEM классы
  - Фильтр-панель с collapse
  - Resize колонок

- ✅ **a012_wb_sales** - Продажи Wildberries
  - TableCheckbox + TableTotalsRow
  - Серверные итоги (total_records, sum_quantity, sum_for_pay, sum_retail_amount)
  - BEM классы

---

## 📚 Референсы

### Эталонные примеры

**Простая таблица:**

```
http://localhost:8080/?active=a002_organization
```

- Файл: `crates/frontend/src/domain/a002_organization/ui/list/mod.rs`
- До 200 записей
- Без пагинации
- Модальное окно

**Сложная таблица:**

```
http://localhost:8080/?active=a016_ym_returns
```

- Файл: `crates/frontend/src/domain/a016_ym_returns/ui/list/mod.rs`
- 1000+ записей
- Серверная пагинация
- Серверные итоги в thead
- Фильтры + экспорт + batch операции

### BEM Референс

Проект использует строгую BEM методологию:

```css
/* Блок */
.table

/* Элементы */
/* Элементы */
.table__row
.table__cell
.table__checkbox

/* Модификаторы */
.table__cell--checkbox
.table__row--selected
.table--striped;
```

**Запрещено:** `.checkbox-cell`, `.tableCell`, `.table-cell`  
**Правильно:** `.table__cell--checkbox`

**Дополнительно:** `E:\dev\bolt\bolt-mpi-ui-redesign\BEM_MIGRATION_MAP.md`

---

## 🔧 Как применять стандарты

### Для новой таблицы

1. **Определить тип:**

   - < 200 записей → Простая
   - > 1000 записей → Сложная

2. **Выбрать эталон:**

   - Простая → скопировать структуру из a002_organization
   - Сложная → скопировать структуру из a016_ym_returns

3. **Backend (для сложных):**

   - Добавить `Totals` структуру
   - Добавить функцию `calculate_totals()`
   - Обновить `PaginatedResponse` с полем `totals`

4. **Frontend:**

   - Использовать `TableCheckbox` для всех чекбоксов
   - Использовать `TableTotalsRow` для итогов (сложные)
   - Добавить `server_totals: Option<Totals>` в state

5. **Проверить BEM:**
   - Все классы `.table__element--modifier`
   - CSS-переменные вместо hardcode
   - Нет inline-стилей

### Для существующей таблицы

1. Добавить импорт:

```rust
use crate::shared::components::table_checkbox::TableCheckbox;
```

2. Заменить чекбоксы:

```rust
// Было
<td class="table__cell--checkbox">
    <input type="checkbox" ... />
</td>

// Стало
<TableCheckbox
    checked=Signal::derive(move || selected.contains(&id))
    on_change=Callback::new(move |checked| toggle(id, checked))
/>
```

3. Для сложных таблиц добавить итоги (опционально):
   - Backend: добавить функцию `calculate_totals()`
   - Frontend: использовать `TableTotalsRow`

---

## 📊 Статистика

### Создано

- **Документов:** 3
- **Backend функций:** 2 (calculate_totals)
- **Frontend компонентов:** 2 (TableCheckbox, TableTotalsRow)
- **CSS классов:** 8 новых BEM классов

### Обновлено

- **Backend handlers:** 2 (a016_ym_returns, a012_wb_sales)
- **Frontend списков:** 6 (организации, маркетплейсы, подключения, возвраты, продажи)
- **CSS файлов:** 1 (components.css)

### Осталось обновить (по желанию)

Можно применить тот же паттерн к:

- a009_ozon_returns
- a014_ozon_transactions
- a015_wb_orders
- a011_ozon_fbo_posting
- a010_ozon_fbs_posting
- a013_ym_order

---

## 🎨 Ключевые принципы

### 1. Два типа таблиц

| Тип         | Когда использовать         | Особенности                          |
| ----------- | -------------------------- | ------------------------------------ |
| **Простая** | Справочники, < 200 записей | Без пагинации, клиентская сортировка |
| **Сложная** | Документы, > 1000 записей  | Серверная пагинация, итоги, фильтры  |

### 2. Всегда с чекбоксами

Во всех таблицах первая колонка - чекбоксы (40px):

- Используется компонент `TableCheckbox`
- BEM классы `.table__cell--checkbox`
- Stop propagation (не открывает detail)

### 3. Итоги от сервера

Для сложных таблиц:

- Рассчитываются на сервере по всему датасету
- Отображаются в thead через `TableTotalsRow`
- Легко включить/выключить (if true/false)
- Учитывают текущие фильтры

### 4. BEM везде

Строгое следование методологии:

- `.table__element--modifier`
- CSS-переменные
- Нет inline-стилей
- Нет hardcode значений

---

## 🚀 Преимущества новой системы

1. **Быстрое развитие**

   - Создание новой таблицы: 15-30 минут
   - Копируй эталон, меняй данные
   - Все компоненты переиспользуемые

2. **Единообразие**

   - Все таблицы выглядят одинаково
   - Пользователь знает что ожидать
   - Легко поддерживать

3. **Гибкость**

   - Легко включить/выключить итоги
   - Два уровня сложности
   - Расширяемая архитектура

4. **Качество кода**
   - BEM методология
   - CSS-переменные
   - Переиспользуемые компоненты
   - Типизация

---

## 📖 Быстрый старт

### Создать простую таблицу

1. Скопировать `a002_organization/ui/list/`
2. Изменить структуру данных
3. Настроить колонки таблицы
4. Готово!

### Создать сложную таблицу

1. Скопировать `a016_ym_returns/ui/list/`
2. Backend:
   - Создать `calculate_totals()` функцию
   - Добавить `Totals` структуру
3. Frontend:
   - Изменить state и DTO
   - Настроить колонки
   - Обновить TableTotalsRow содержимое
4. Готово!

### Мигрировать существующую таблицу

1. Добавить `use crate::shared::components::table_checkbox::TableCheckbox;`
2. Заменить чекбоксы на `<TableCheckbox ... />`
3. Проверить BEM классы
4. Для сложных: добавить серверные итоги (опционально)

---

## 🎓 Обучение

### Для разработчиков

**Обязательно прочитать:**

1. [`table-standards.md`](memory-bank/architecture/table-standards.md) - основной документ
2. [`detail-form-standard.md`](memory-bank/architecture/detail-form-standard.md) - формы
3. `E:\dev\bolt\bolt-mpi-ui-redesign\BEM_MIGRATION_MAP.md` - BEM референс

**Эталоны для изучения:**

- Простая: `a002_organization/ui/list/mod.rs`
- Сложная: `a016_ym_returns/ui/list/mod.rs`

### Частые вопросы

**Q: Когда использовать простую таблицу?**  
A: Для справочников и классификаторов с количеством записей до 100-200.

**Q: Как отключить строку итогов?**  
A: Изменить `if let Some(totals)` на `if false` или закомментировать блок.

**Q: Можно ли использовать inline-стили?**  
A: Только для динамических значений (width при resize). Для всего остального - CSS классы.

**Q: Как проверить BEM?**  
A: Все классы должны содержать `__` или `--`. Нет классов типа `.checkbox-cell`.

---

## 🔍 Проверка качества

### Чеклист перед коммитом

Простая таблица:

- [ ] Используется `TableCheckbox`
- [ ] BEM классы (`.table__element--modifier`)
- [ ] Нет inline-стилей
- [ ] CSS-переменные вместо hardcode
- [ ] Модальное окно для редактирования
- [ ] Загрузка всех записей без пагинации

Сложная таблица:

- [ ] Используется `TableCheckbox`
- [ ] Используется `TableTotalsRow` (если нужны итоги)
- [ ] BEM классы
- [ ] Серверная пагинация
- [ ] Серверные итоги в state
- [ ] Фильтр-панель
- [ ] Resize колонок (опционально)

---

## 💡 Следующие шаги

1. **Протестировать обновлённые таблицы**

   - Открыть каждую таблицу в браузере
   - Проверить чекбоксы
   - Проверить итоги (для сложных)

2. **Обновить остальные списки** (опционально)

   - По тому же паттерну
   - Следуя table-standards.md

3. **Обновить формы детальных записей** (следующий этап)
   - Следуя detail-form-standard.md
   - Использовать field-row компонент

---

## 📞 Контакты

**Документация:** `memory-bank/architecture/`  
**Эталоны:** `a002_organization`, `a016_ym_returns`  
**Компоненты:** `crates/frontend/src/shared/components/`

---

**Версия:** 1.0  
**Дата:** 2025-12-19  
**Статус:** ✅ Готово к использованию
